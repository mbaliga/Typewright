# app-android

The Android application shell: one Activity that hosts the shared `TypewrightApp()` composable, with no logic of its own. There is no phone or emulator in the build environment, so on-device behaviour is verified by the owner only.
