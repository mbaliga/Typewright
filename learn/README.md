# learn

The instruments of the Learn room: the scrapbook and moodboard as project files, the comparison overlay and the anatomy lens (brief §9, handoff M4). Still a P0 placeholder (`LearnModule.kt`, one object naming the module): the real overlay, anatomy lens and scrapbook logic and data currently live in `ui` (`ui/src/commonMain/kotlin/dev/aarso/typewright/ui/learn/`), not here.
