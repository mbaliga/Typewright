# V1 triage of the open questions

Every item in `docs/OPEN_QUESTIONS.md`, 1–125, placed in exactly one category for V1
(`PROMPTS_V1.md` P10 step 4, 25 Sep 2026). What V1 is: `docs/V1_SCOPE.md`.

- **V1-BLOCKING**: V1's golden path, or a V1 screen, can't ship correctly until it is done.
  Each names the prompt that closes it.
- **MADHAV**: needs an owner decision. Each maps to one of PROMPTS_V1 §2's D1–D9, or to a new
  D10–D14 below.
- **DEFERRED**: real, but V1 doesn't need it.
- **RESOLVED**: closed in the code. Each cites the commit and the file or test that shows it.

Items 1–120 were triaged by six independent readers. Each read PROMPTS_V1 and SCREENS_V1 in
full and checked every claim against the code, not the item's own text. The lead then merged
their results and adjusted two of them (items 2 and 119). Items 121–125 were added in P10.
File references are to the renamed packages (`com.asoc.typewright`).

## Counts

| Category | Items |
|---|---|
| V1-BLOCKING | 31 |
| MADHAV | 16 |
| DEFERRED | 40 |
| RESOLVED | 38 |
| **Total** | **125** |

## V1-BLOCKING, by the prompt that closes it

| Prompt | Items |
|---|---|
| P11 | 75, 91, 93, and 124 and 125 with P12 |
| P12 | 51, 66, 67, 86, and 124 and 125 with P11 |
| P13 | 56, 65, 79, 80, 82, 89, 94, 119 |
| P14 | 8, 18, 92 |
| P16 | 6, 20, 21, 22 |
| P17 | 25, 29, 30 |
| P18 | 13, 14, 31 |
| P19 | 48 |

## Decisions owed

| D | Decision | Items | Needed by |
|---|---|---|---|
| D1 | Licence: the lawyer's read. Item 122 adds that app code pushed before bb321f5 is Apache-2.0 for anyone who copied it. | 122 | before v1.0.0 |
| D4 | Web V1 compile | 10 | before P14 |
| D7 | Screen designs: the four SCREENS_V1 §8 points; the Scrapbook's tilt; **the `ui/v1-screens/` mockup sources, which are not in the repository** | 77, 123 | **before P11's UI** |
| D8 | The bundled UI face (IBM Plex Sans and Mono recommended) | 9 | before P13 |
| D9 | minSdk 31 | 4 | before P18 |
| **D10** (new) | Does law 3 cover build tooling? Setting `kotlin.internal.collectFUSMetrics=false` in gradle.properties stops the Kotlin Gradle plugin writing local build statistics. | 15 | any time; not blocking |
| **D11** (new) | Keep the 17 Learn font binaries committed, or move them to Git LFS or a build-time fetch | 32 | before P18 |
| **D12** (new) | Sign off the P6 Learn content calls: the Transitional bank entry that can never surface, the Slab give-away wording, the vocabulary scene's staging, the Garalde stress dial and three Lens terms outside the scaffold's vocabulary. Item 41 is also a visible contradiction on S18: the Garalde dial says "no stress axis" and its caption says "oblique stress". | 35, 37, 38, 40, 41, 52 | before P19 |
| **D13** (new) | The Devanagari template pack's duplicate sheets 011 and 056: replace them with the missing letters or keep them as spares | 96 | before P16 prints Devanagari sheets |
| **D14** (new) | Whether using google/fonts' unlicensed `tags/all/families.csv` at build time needs permission | 121 | any time; not blocking |

D2 (name clearance, before P18), D5 (the Android compile route, after P14's spike) and D6
(Workbook in V1) have no open question of their own.

## Gaps no V1 prompt names

The triage found these (and the golden-path harness found item 125). Each is assigned to the nearest prompt, and that prompt should add
it to its own work.

1. **Glyphs carry no Unicode code points** (item 124). `core-geometry`'s `Glyph` has no
   unicodes, and `core-font`'s GlifCodec drops `<unicode>`. An imported font loses its cmap,
   and a compiled font would map no characters. That blocks golden-path steps 1a and 5.
   **P11** (the project model) and **P12** (fill them on import).
2. **The UFO writer is cubic-only** (item 125). `writeGlif` refuses TrueType's quadratic
   contours, so a font opened from a TTF can't be saved as a project. P11 and P12 must choose
   between writing `qcurve` points and elevating to cubic on import; law 1 favours keeping the
   source outline. **P11 + P12**.
3. **The browser build can't load any bundled data** (items 8, 18, 47, 87, 92, 95). The
   wasmJs readers in `qa:corpus`, `campaign` and `learn:scenes` use Node's `fs` and fail in a
   browser. So on the web preview, layer-two Check, Economy, the Workbook and the Learn scenes
   are all unavailable, and P19 runs the golden path on that preview. **P14**; P18's static
   web build is the fallback.
4. **Shaping preview reaches no screen.** `ui` declares dependencies on `:scripts` and
   `:shape-preview`, but no `ui` or app source imports either. PROMPTS_V1 §1 ("Kana,
   Devanagari and Naskh in Draw/Space with shaping preview") and SCREENS_V1 §4 ("Scripts …
   built today: yes") overstate what is built, and journey J4 is unreachable. **P17**
   (the Space audit).
5. **Draw's construction tools aren't in the UI either.** SCREENS_V1 §4 lists pen, Hobby
   curves, primitives, booleans and transforms as built in the sheet. `ui` imports no
   Booleans, HobbySpline, Transform or Offset code, and primitives are never drawn as ink.
   **P17**.
6. **The puck's gestures read the wrong coordinate space** (item 119). The composable feeds
   puck-local positions to a machine tested with canvas-space ones. So grip-drag is dead on
   every platform, and the hold radial's hub centre is probably wrong too. **P13**, before
   the control deck re-pins the puck.
7. **The fitter's fence targets are not in P16's acceptance** (items 20, 21). SCREENS_V1
   S10's "done when" requires T 8·0, o 16·16, n 14·8, H 12·0. P16's acceptance lists only
   golden-path steps 1–2 and 95% cell mapping. Today the fitter gives T 8/16, H 21/42,
   n 23/46, o 37/74. **P16** should add the fence counts.
8. **The Workbook's Task 4 style is hard-coded** (item 18). `DEFAULT_TASK4_STYLE_KEY =
   "sans-geometric"` should become the class the user declares in S02, which is stored in
   typewright.json. **P12**, where the Workbook gates move onto the open project.
9. **The Overlay compares the wrong letters** (item 67). S18's "done when" (J3: "your o
   against Jost and Work Sans with the redline probe") doesn't match the Overlay, which redlines a fixed H
   against EB Garamond and Libre Baskerville. **P12**.
10. **The web compile stub's message contradicts D4.** `HostedEndpointBackend` says "Planned:
   upload the project to the hosted endpoint", but D4 says the web V1 has no compile. Also,
   D4's own reason is stale: CLAUDE.md law 3 was amended in bb321f5 to allow an opt-in
   hosted build. **P14** step D.

## All items

"Closes / decision / evidence" holds the prompt for V1-BLOCKING, the decision for MADHAV,
and the commit and evidence for RESOLVED.

| # | Item | Category | Closes / decision / evidence | Why |
|---|---|---|---|---|
| 1 | Root LICENSE versus brief §15; licence undecided | RESOLVED | bb321f5: LICENSE is FSL-1.1-ALv2 with LICENSES/Apache-2.0.txt and CC0-1.0.txt; tools/check_licences.py:8 enforces the dependency direction and passes today; CI runs it at .github/workflows/ci.yml:28 | The licence split is applied, and the check script and CI step exist and pass on this branch. |
| 2 | Package and application id com.asoc.typewright are provisional | RESOLVED | 4cff7c7: application id, packages and Maven group renamed (app-android/build.gradle.kts applicationId "com.asoc.typewright"; build-logic ProjectExtensions.kt TYPEWRIGHT_GROUP "com.asoc") | P10 step 2 did the rename; all three CI jobs pass on it. |
| 3 | Explorer HTML lacks [hidden] rule, renders every screen standalone | DEFERRED |  | The explorer is unchanged (one commit, 493b5f5), but tools/explorer-shots.mjs:26 injects the rule, and V1 screens take their look from ui/v1-screens/, so this is an owner one-liner in the chat app that V1 doesn't need. |
| 4 | minSdk 31 (Android 12) excludes older phones | MADHAV | D9 | The code already uses minSdk 31 (gradle/libs.versions.toml:16), but D9 is still listed as owed by Madhav before P18. |
| 5 | Shaper contract exceeds what Android and browser stacks expose | RESOLVED | e384d88 (web) + 48293a7 (desktop/Android): Shaper.kt:63-66 makes clusters nullable, AndroidTextRunShaper.kt:110 returns clusters=null, web keeps the honest stub plus the render-only BrowserFontFaceVisualPreview.kt:81 renderPng, and BrowserFontFaceVisualPreviewBrowserTest covers it | The "decide in P8/P9" question was decided and built: full clusters on desktop, nullable clusters on Android, a render-only preview on web, and no JNI HarfBuzz (DECISIONS D14). |
| 6 | Where Android camera/OpenCV capture code lives, given pure engine-trace | V1-BLOCKING | P16 | Capture isn't built: there's no fiducial, homography or cell detection and no capture-android module (settings.gradle.kts), and P16 step 1 names OQ 6 and settles the placement while building capture, which golden-path step 1 needs. |
| 7 | Fontbakery/Fontspector bridge needs a runtime qa can't hold | RESOLVED | 0a5483c: interface at qa/src/commonMain/.../qa/LayerOneChecker.kt:23 with platform actuals (FontspectorCliChecker at LayerOneChecker.jvm.kt:190, honest web Unavailable at LayerOneChecker.wasmJs.kt:30); FontspectorCliCheckerTest | The architectural question (an interface in pure qa, runtimes in platform actuals, no layer one on web) was decided and built. The remaining work (a fontbakery fallback on desktop, the Android route) is P14's own scope, not this item. |
| 8 | Node-economy corpus can't load in the browser Wasm build | V1-BLOCKING | P14 | The wasmJs actual (qa/corpus CorpusResources.wasmJs.kt:28) needs Node's process and fails in a browser, and the Workbook already reports Unavailable on web for this reason (WorkbookCampaignSnapshot.kt:44-53). Web's layer-two Check and the Economy view (S14/S15) need the corpus. No prompt names this; P14 is the nearest. |
| 9 | No bundled monospace/UI face; web falls back to Compose sans | MADHAV | D8 | The UI still uses FontFamily.Monospace (ui/tokens/Typography.kt:48) with no bundled face. D8 names OQ 9 and recommends IBM Plex Sans/Mono, but Madhav hasn't confirmed it yet, and it's needed before P13. |
| 10 | Who runs the hosted build endpoint | MADHAV | D4 | HostedEndpointBackend.kt:21 still defaults endpoint=null. D4 (web V1 = preview with no compile, hosted endpoint later as the paid product) answers it, but Madhav still owes D4 before P14. |
| 11 | Composite builds with Shared-Libraries-asoc force AGP/Kotlin lockstep | DEFERRED |  | Typewright doesn't depend on that repository (settings.gradle.kts only includes build-logic), and the item only matters "if Typewright ever needs it". |
| 12 | compileSdk 37, targetSdk held at 36 until device verification | RESOLVED | eb05aa7: gradle/libs.versions.toml:14-15 set compileSdk 37 and targetSdk 36 with the reason in the comment at lines 11-13 | The decision is taken and built. P18 only restates "targetSdk stays 36 unless owner-verified", and raising it to 37 is post-V1 owner work on a device. |
| 13 | Web app goes blank under non-standard browser locale (en-US@posix) | V1-BLOCKING | P18 | app-web/src/wasmJsMain/resources/index.html still has no navigator.languages guard, and P18's web bullet explicitly asks for "the locale guard from OQ 13". P19 also tests non-ASCII locales. |
| 14 | Licences/notices page for Skia, FreeType and other bundled binaries | V1-BLOCKING | P18 | No About or licences screen exists. The design now exists (S23, ui/v1-screens/About, which answers the item's law-6 objection), and P18's first bullet names OQ 14 and the Skia/FreeType/libpng notices. P13 builds only the S23 shell. |
| 15 | Kotlin Gradle plugin writes local build statistics | MADHAV | D10 | gradle.properties still leaves the flag unset. It's a scope-of-law-3 call for the owner that touches no shipped binary, so it doesn't block V1. |
| 16 | Compose UI code has no Wasm browser tests (CMP-4906) | RESOLVED | cbbd182: ui/build.gradle.kts:22-30 runs ui's commonTests in headless Chrome through Karma (useChromeHeadlessNoSandbox), the Node route being blocked by CMP-4906 (explained at lines 66-81) | ui got the browser-run Wasm tests the item asked for. The only gap is that CI's web job (ci.yml:80) runs :app-web:wasmJsBrowserTest but not :ui:wasmJsBrowserTest. |
| 17 | Explorer's node-economy worked examples stale after P0c corpus regeneration | DEFERRED |  | The explorer still holds the pre-P0c ECON data (for example sans-geometric T q3 8.8, where the corpus now says 12.5), but no app code copies it. V1's Check and Economy follow ui/v1-screens and S15's regenerated numbers, so the owner can fix the explorer in the chat app after V1. |
| 18 | Corpus loader: browser half unsolved; Task 4 comparison box open | V1-BLOCKING | P14 | Its Task 4 bullet was closed by b3840dd (campaign WorkbookProgress.kt:19 DEFAULT_TASK4_STYLE_KEY="sans-geometric"), but its main open point, loading the corpus in a real browser, is still unbuilt and is the same gap as item 8, which P14's web Check and Economy need. |
| 19 | Style detector gets only 8/19 top-1; CJK font OOM | DEFERRED |  | No V1 screen calls rankStyleClasses (it has no callers in ui, campaign or learn). S02 has the user declare the class through chips, and the Lens uses only the feature extractors, so improving inference accuracy (brief §8.4's infer layer) is post-V1. |
| 20 | Fitter writes straight runs as curves; T fits 8·16, not 8·0 | V1-BLOCKING | P16 | The CLAUDE.md fence (T 8·0·8, H 12·0·12) is still unmet: Contour.kt still has no line kind, FitPipelineHyleDecoValidationTest still reports T 8/16, and CubicFitting emits nearly-collinear handles, which GlifCodec writes as type="curve" instead of its line convention (control1=start, control2=end). S10's Trace screen shows this output against the box, so P16, which builds Trace on engine-trace, has to close it, most simply with a Snap-stage pass that collapses each collinear cubic to that line convention. |
| 21 | Snap-stage extrema insertion leaves H, n, o far over fence targets | V1-BLOCKING | P16 | S10's 'Done when' needs o 80 → 16 on · 16 off with fixtures unchanged, but the pipeline still gives o 37/74, H 21/42 and n 23/46 (FitPipelineHyleDecoValidationTest KDoc, TypeConstraints.kt unchanged since df619b4), so the Trace screen would mark the app's own traces as outliers. The side question of a fifth 'arch' construction class is a small product point that V1 doesn't need. |
| 22 | Trace threshold hollows wide strokes; stroke-width estimate fragile on multi-blob rasters | V1-BLOCKING | P16 | DEFAULT_ADAPTIVE_THRESHOLD_WINDOW_RADIUS is still 15 px and estimateStrokeWidth still takes the global max (engine-trace unchanged since 9954b99). A marker stem in a phone photo of a 24-cell sheet is easily wider than 30 px and will threshold hollow. P16 step 1 names the stroke-width estimate and real photo fixtures, so calibrating the window from the estimate belongs there. The marching-squares chamfer bullet is inherent and needs no action. |
| 23 | Hobby splines use Bessel tangents, not literal mp_make_choices | RESOLVED | 85c6ccc: engine-construct/src/commonTest/kotlin/com/asoc/typewright/engine/construct/HobbySplineTest.kt:36 circleKnotsMatchKnownKappaRatioAtExactPrecisionInternally (plus :74 general n-gon kappa, :155 tension) | This records a verified substitute that was implemented and tested, plus the curl and degenerate-offset rules, with nothing left to do. A literal METAFONT port is an optional future revisit, and wiring Hobby into the pen tool is P17's Draw audit, not this item. |
| 24 | Booleans verified only on fixtures; oblique derivation; Spiro dropped | DEFERRED |  | No golden-path step needs a boolean op, and the puck's Boolean tool (Tool.BOOLEAN) isn't wired to Booleans.kt: ui imports no engine-construct boolean. Oblique derivation is M6 Derive, and Spiro was dropped for v1 in favour of Hobby alone. Hardening against glyph-shaped, near-tangent input is v1.1 work, unless P17 decides to expose the B tool, in which case it must also catch Booleans.kt's IllegalStateException. |
| 25 | Sheet gaps: no = entry, object radial, pointer kinds, haptics, depth | V1-BLOCKING | P17 | P17 names numeric `=` expressions and the Hold-object radial outright, and neither exists in ui. The rest of its Draw audit against brief §5 also has open gaps: no PointerType anywhere in ui, Puck.kt:163 handles RadialDetentTicked as a no-op so haptics are unwired, and SheetCamera.depth is never read. The command-palette bullet is stale because a053ef7 built it (CommandPalette.kt). |
| 26 | Eight palette commands as pure functions; open-contour and harmonise conventions | RESOLVED | 7162a60: core-geometry/src/commonTest/kotlin/com/asoc/typewright/core/geometry/PaletteTest.kt:17 registryHasExactlyTheEightBriefNamedCommands (Palette.kt:39 PaletteCommand registry) | This records the decisions (no 'open' for an always-closed Contour, harmonise needs an explicit fixed side, round-coordinates is an identity on Int points), all implemented and tested. Wiring the commands to a live selection is tracked by item 30 (P17). |
| 27 | Serif and Terminal primitives unbuilt, awaiting a font-wide serif style | DEFERRED |  | The construction grammar is M2, and the V1 golden path (M0+M1) needs no Serif or Terminal primitive. Designing the serif-style parameter set is an owner decision, but not one V1 needs, and least-squares fitting of ellipses to points is also optional. |
| 28 | UFO guidelines, kerning groups, kerning pairs and .fea round-trip | RESOLVED | dabb9bc: core-font/src/commonTest/kotlin/com/asoc/typewright/core/font/ufo/UfoKerningTest.kt:42 matchesTheUfo3SpecsOwnGroupsPlistExample and :109 matchesTheUfo3SpecsOwnKerningPlistExample; GlifCodecTest.kt:336 matchesTheUfo3SpecsOwnGlifGuidelineExample | The codec-level round trip this item reports is built and tested against the spec's own examples. What's left belongs elsewhere: the group side-uniqueness check at edit time goes with the Space Groups UI in P17, and non-default layers go with item 29. |
| 29 | Puck, primitives menu, inspector and layers wired, with scope cuts | V1-BLOCKING | P17 | S11 lists primitives and layers as built, but a primitive makes a default instance that is never drawn as ink and can't be baked (nothing sets baked = true). The layers panel loads no image and persists nothing, which misses brief §10 v1 must-have #3, and DEFAULT_FONT_WIDE_STEM is still Hyle Deco's 44 (PrimitivesConstruction.kt:253). P17's audit of Draw against the §10 must-haves is where a reader expects this closed. The orchestrator layers-overlap flag is stale: only 736ae73 added Layers.kt. |
| 30 | Guides, Space kerning/.fea and palette run on sample data | V1-BLOCKING | P17 | P17 explicitly says to wire every palette entry left with run = null, and CommandPalette.kt:301 and :308 are still null. The other commands still run on demoSquare and demoCircle. Guides and kerning come from sampleGuidelines() and sampleKerning() (TypewrightSheet.kt:128, :164), guides have no creation gesture (a §10 v1 must-have), and .fea is read-only. P12 does the swap to the open project. |
| 31 | Roboto Slab is Apache-2.0, not OFL; needs a per-face licence listing | V1-BLOCKING | P18 | LICENSING.md:18 (bb321f5) already says bundled fonts are 'listed in About (S23)' per face, but About doesn't exist yet, and data/LICENSE:7 (added in bb321f5) wrongly says every learn-faces family sits under an OFL.txt, when robotoslab/ has LICENSE.txt (Apache-2.0). P18 builds About and checks that P10's licence work is complete. |
| 32 | Learn font binaries are committed to git; confirm long-term storage pattern | MADHAV | D11 | The item asks the owner to choose a repository policy, and no D1-D9 covers it. It doesn't block V1: the 5.8 MB of committed faces work today, and data/LICENSE now records their licences. |
| 33 | Browser wasm loading of data/learn-faces is still open | DEFERRED |  | Commit fc0d85e settled the JVM, Android and Node half (LearnFaceResources.kt), but in the browser the web still can't load the faces (LearnFaceFonts.wasmJs.kt:16 returns false). What's left is item 47, and the web is only a labelled preview under D4. |
| 34 | A re-fetch must re-derive each family's licence, not assume it | RESOLVED | 3676c9d: data/scripts/fetch_learn_faces.py:252-253 re-reads METADATA.pb's license field on every run and maps it via LICENSE_FIELD_MAP | The mechanism the item asks for is in the code. All that's left is a standing note to hand-diff THIRD_PARTY.md on a future re-fetch, which is process, not open work. |
| 35 | Identify-it bank names Libre Baskerville, already on stage | MADHAV | D12 | Same issue as item 37. The content author kept the entry verbatim (identify-it-bank.yaml:28) and passed the choice to the owner, and StrandSequencer filters it out, so V1 isn't blocked. |
| 36 | Whole-block reading of the 'face used on stage' rule | RESOLVED | a2b8426: LineagesTab.kt:102 runs StrandSequencer.plan over the whole block and :138 shows the quiz only after the last scene | The Learn UI half of P6 was built on the whole-block reading the item asked someone to confirm, so the question is settled in the code. |
| 37 | Transitional bank entry can never surface; only 7 of 8 questions are live | MADHAV | D12 | The owner has to pick a real OFL Transitional face or accept 7 questions (LineagesSceneContentTest.kt:223 proves the exclusion). It's content, not engineering, and V1 can ship with 7. |
| 38 | Roboto Slab bank give-away phrasing is authored, not taken from the scaffold | MADHAV | D12 | This asks the owner of the LESSONS_SCAFFOLD content to confirm or supply one sentence. It's a content call and doesn't block V1. |
| 39 | Exercise bank entries have no word or options fields | RESOLVED | a2b8426: LineagesQuizItems.kt:66-72 gives each bank entry the word Hamburgefonstiv and synthesised options; LineagesQuizItemsTest | The Learn UI filled the gap the item describes. Whether the synthesised options are good enough is tracked separately as item 59, outside this range. |
| 40 | Vocabulary scene staging was authored by the task and needs confirmation | MADHAV | D12 | The owner has to confirm how 07-vocabulary.yaml stages the 'gothic' ambiguity. It's lesson content and doesn't block V1. |
| 41 | Era 2 Garalde has null stress; the dial's debut is unconfirmed | MADHAV | D12 | Choosing null or a static 30° is a content call, but the null reading now shows a visible contradiction on S18: the Garalde dial reads 'no stress axis' (LineagesQuizItems.kt:142, from SceneRenderer.kt:74) while its caption says 'oblique stress' (02-garalde.yaml:28). A static 30° is the likely answer. |
| 42 | Concurrent Scene schema extensions compose cleanly | RESOLVED | 52d4b4e: Scene.kt:71 (scaffold) and Scene.kt:113/167 (FaceSource, Stage.pipeline) coexist; CraftSchemaExtensionTest and ScaffoldFieldTest pass | It was a heads-up for the P6 reviewer. Both extensions landed together in one commit with tests, so nothing is left. |
| 43 | Craft schema extension done; Stage.pipeline has no consumer yet | DEFERRED |  | The schema is done in 52d4b4e (CraftSchemaExtensionTest). What's left is rendering Craft scenes live, and S18's four toggles have no Craft tab, so that isn't a V1 screen. |
| 44 | A1 scene: fitted T still has 16 off-curve points, an outlier | DEFERRED |  | A1 is a Craft scene that no V1 screen renders, and its caption is honest as written. The underlying CUBIC type gap is item 20, which is where it should be triaged (see notes). |
| 45 | B1 has no bold fixture; C1 has no composite rebuild | DEFERRED |  | A drawn bold needs masters or interpolation (out of V1), and anchor-driven composites are brief §10 v1.5. core-font already decomposes composite glyphs on read (GlyfTable.kt), so importing a TTF isn't blocked. |
| 46 | B2 residual is 0.145; the scene says about 0.2 | DEFERRED |  | b2-shearing-an-italic.yaml:51/60 still says 0.2. It's a wording fix in a Craft scene that no V1 screen shows. |
| 47 | Browser wasm cannot fetch Learn face bytes | DEFERRED |  | Under D4 the web is a labelled preview, and LineagesTab.kt:141 already shows an honest 'system font instead' note. Desktop and Android, the full V1 targets, load the real faces. |
| 48 | Android Learn font loading is unverified on a device | V1-BLOCKING | P19 | Android is a V1 target, and learnFaceFontsAreReal() returns true there, so a failed load would silently fall back to the default font. Only the owner's device run can close it. Learn isn't a golden-path step, so P19's Android checklist must add it explicitly. |
| 49 | Non-default weights from variable Learn faces are unverified | DEFERRED |  | No scene or caller asks for a non-default weight or style today (the only callers are LineagesTab.kt:196-197 and :480, all at the default weight), so nothing in V1 exercises it. |
| 50 | qa:corpus style functions were internal; widened to public | RESOLVED | 1380390: ContrastStress.kt:57 changed from 'internal fun contrastRatio' to public, with the other listed declarations | The widening is done and in use. The item's remaining advice (check visibility before wiring) is guidance, not open work. |
| 51 | The Lens's 'your letter' is the Hyle Deco fixture, not the project | V1-BLOCKING | P12 | LensTab.kt:78 still reads HyleDecoProjectFontBytes, and P12 step 2 names the Anatomy Lens among the HyleDeco reads it must replace with the open project. |
| 52 | Three Lens terms are outside the scaffold's 26-term vocabulary | MADHAV | D12 | The item asks the product owner outright whether STOREYS, ROUNDNESS and X_HEIGHT_TO_CAP_HEIGHT_RATIO (AnatomyLensData.kt:136-137) ship in the Lens. They work as they are, so V1 isn't blocked. |
| 53 | SerifKind never reports hairline serifs | DEFERRED |  | It would need a new measured feature in the style detector. The Lens already flags serif kind as 'our heuristic' (AnatomyLensData.kt:498), so law 5 is met for V1. |
| 54 | Os2Table skipped xAvgCharWidth, so every OS/2 field was misread | RESOLVED | 1d026b2: Os2Table.kt:42 skips xAvgCharWidth; HyleDecoCrossCheckTest.os2XHeightAndCapHeightAreReadFromTheRealSpecOffsets (line 114) and Os2TableTest | The parser bug is fixed, with byte-level and synthetic regression tests. |
| 55 | No existing qa/corpus test asserted Hyle Deco's anatomy values | RESOLVED | 1380390: AnatomyLensDataHyleDecoTest.kt:77-96 pins contrast 1.2513 and stress 52.5 against direct qa/corpus calls | It records a mismatch in the instructions, and the regression test the item describes exists. Nothing is left to do. |
| 56 | Learn is reached by a corner button, not a router | V1-BLOCKING | P13 | The Learn screen exists (4523ab1), but it's reached only through TypewrightApp.kt's corner buttons. PROMPTS_V1 §0 cites OQ 56 for that, and P13 step 2 says 'Close OQ 56/82/89' once the deck and router replace them. |
| 57 | Lineages scrubber is per-scene, explorer mock is block-wide | DEFERRED |  | SCREENS_V1 S18 ships P6's four tabs 'as they are today' and LineagesTab still scrubs per scene, so the product confirmation can wait for v1.1 (unless the unseen ui/v1-screens Learn mockup shows a block-wide scrubber, in which case it becomes P13 work). |
| 58 | Identify-it quiz gate and all-cards-at-once stacking unconfirmed | DEFERRED |  | The quiz works on real content, S18 keeps Learn as it is today, and paging and the gate are refinements nothing in V1 needs. |
| 59 | Quiz distractor options synthesised, bank has no options field | DEFERRED |  | It is a lesson-content schema change (curated options, tied to item 39), and V1 can ship the deterministic real-class-name distractors. |
| 60 | Stage.align ignored, crossfade lacks x-height size compensation | DEFERRED |  | This is visual polish on the Lineages crossfade, and the fix is unscoped metric reading that V1's Learn (S18 'as today') doesn't need. |
| 61 | No Lineages autoplay; Scene.duration and normalizedScrub unused | DEFERRED |  | The explorer has no play control either, so autoplay is a new feature V1 doesn't require. |
| 62 | Scene callouts rendered as improvised violet chip, no explorer design | DEFERRED |  | The chip is a law-6 improvisation that eventually needs an owner-drawn callout look, but it only appears in one Lineages scene and S18 ships Learn as it is today. |
| 63 | learnFaceFontFamily unguarded entry() read crashed wasmJs, fixed | RESOLVED | a2b8426 - ui/src/commonMain/kotlin/com/asoc/typewright/ui/learn/LearnFaceFonts.kt:72 wraps LearnFaceResources.entry(key) in runCatching | The code fix is in place; the only open part is a real-browser confirmation of the fallback, which P19's run of the web preview covers. |
| 64 | Os2Table missed xAvgCharWidth skip, misread every later field | RESOLVED | 1d026b2 - core-font/.../sfnt/Os2Table.kt:42 cursor.skip(2); HyleDecoCrossCheckTest.os2XHeightAndCapHeightAreReadFromTheRealSpecOffsets | The parser fix and a real-font regression test are both committed, and the item itself says no action is needed. |
| 65 | Overlay comparison layers amber/magenta, explorer uses violet/cyan | V1-BLOCKING | P13 | SCREENS_V1 §3.1 settles the disagreement by making 'Comparison layers 1-4' their own themeable roles with line shapes, so what's left is P13 step 5 moving OverlayDemoLayers' hard-coded MeaningColors.AMBER/MAGENTA onto those roles (P13's test fails on hard-coded colours). |
| 66 | Hyle Deco bytes embedded as base64 in ui commonMain | V1-BLOCKING | P12 | HyleDecoProjectFontBytes.kt still feeds Overlay's 'your font' layer, which P12 step 2 replaces with the open project and P12 step 3's HyleDeco grep test forbids. |
| 67 | Overlay redline fixed to 'H'; 'per glyph ›' does nothing | V1-BLOCKING | P12 | S18's 'done when' is J3 ('your o against Jost and Work Sans with the redline probe'), but OverlayTab.kt:319-324 redlines only 'H' in 'Hamburg' against EB Garamond and Libre Baskerville; no prompt names this explicitly, and P12 (which rewires Overlay to the open project) is the nearest, with the Compare deep link arriving with P13's router. |
| 68 | Overlay word field wraps 'Hamburg' mid-word at phone width | DEFERRED |  | This is cosmetic layout polish that V1 can ship with, and J3's focused-glyph change (item 67) may change the default word anyway. |
| 69 | toComposePath filled counters solid under NonZero; now EvenOdd | RESOLVED | c3f3780 - ui/src/commonMain/kotlin/com/asoc/typewright/ui/GeometryInterop.kt:100 path.fillType = PathFillType.EvenOdd | The shared bridge sets even-odd fill for every caller, and the item is a record of that fix. |
| 70 | Lens switches hero glyph per term group, not one static 'a' | DEFERRED |  | This is a product confirmation about Lens presentation, and S18 ships the Lens as it is today, so it waits for v1.1. |
| 71 | Storeys diagram always draws 'a' though value may come from 'g' | DEFERRED |  | The measured number is correct and only the illustrating glyph can differ, which is a small diagram refinement V1 can ship without. |
| 72 | Lens leader lines use fixed ink-bounds fractions, not probe points | DEFERRED |  | This is polish that needs a qa/corpus visibility widening; its risk rises once P12 puts user fonts in the Lens, but a slightly misplaced leader line doesn't block J3. |
| 73 | Leader line stroke itself has no tap target | DEFERRED |  | The dot and label targets already work, and a hit area along the thin stroke is minor polish. |
| 74 | Compose Row starvation bug in Scrapbook header and caption, fixed | RESOLVED | 0100722 - ScrapbookTab.kt:193 weight(1f, fill = false) on header label; PinCaption (ScrapbookTab.kt:446-465) stacks title over source | Both instances are fixed in code; the item's description of the caption fix (weight 3f/2f) is stale, since the code now stacks the two lines instead. |
| 75 | Scrapbook '+ note' invented, pins held in memory only | V1-BLOCKING | P11 | P11 explicitly moves Scrapbook notes from memory into the project (scrapbook/manifest), and ScrapbookManifestCodec is ready for it; the improvised NoteDraftRow look is a secondary law-6 gap. |
| 76 | Pin board uses greedy fixed-height column split, not masonry | DEFERRED |  | This is a layout-fidelity refinement (swap in LazyVerticalStaggeredGrid) that only shows on boards with many notes of uneven length. |
| 77 | Scrapbook pins tilted though explorer's #ln-scrap board is flat | MADHAV | D7 | The P6 prompt asked for a tilt that the explorer doesn't draw, so the owner of the look source has to choose; S18's 'Scrapbook keeps the explorer's look' reads as flat, and after that the change is one line. |
| 78 | SampleScrapbook photo pin ids chosen to vary pattern variants | RESOLVED | 0100722 - ui/src/commonMain/kotlin/com/asoc/typewright/ui/learn/ScrapbookManifest.kt:144-174 SampleScrapbook ids | This is a record of a cosmetic sample-data choice with no action asked; the sample itself will go when P11/P12 replace fixtures with the open project. |
| 79 | LearnScreen uses its own LearnHeaderBar with back button | V1-BLOCKING | P13 | The standing rule says no screen adds its own back button, tab row or title bar, and P13 step 3 moves Learn onto the control deck, which replaces LearnHeaderBar (LearnScreen.kt:88,108). |
| 80 | Learn's '›_' commands button visible but deliberately unwired | V1-BLOCKING | P13 | P13 builds the ⋯ page menu (S20) whose first item is 'Command…' (C7) and moves Learn onto the deck, which replaces this inert button (LearnScreen.kt:142-144). |
| 81 | Learn tab row Row starvation fixed with horizontalScroll | RESOLVED | 4523ab1 - ui/src/commonMain/kotlin/com/asoc/typewright/ui/learn/LearnScreen.kt:163 .horizontalScroll(rememberScrollState()) | The fix is in place and screenshot-checked; P13 replaces the tab row with the toggle arc anyway. |
| 82 | Entry point is a corner Learn tab plus TypewrightAppNavState | V1-BLOCKING | P13 | P13 step 2 explicitly removes the corner buttons, builds the router and says 'Close OQ 56/82/89'. |
| 83 | Tab and entry tests assert on state classes, not pointer events | RESOLVED | 4523ab1 - ui/src/commonTest/.../TypewrightAppNavStateTest.kt and ui/src/commonTest/.../learn/LearnScreenStateTest.kt | This is a record of a testing convention that was implemented as described, with no action asked; no test in the repo sends pointer events yet. |
| 84 | LearnTabButton selection uses padding, not explorer's negative margins | RESOLVED | 4523ab1 - ui/src/commonMain/kotlin/com/asoc/typewright/ui/learn/LearnScreen.kt:214-216 background(ink) plus fixed padding | This is a documented few-dp simplification with no action asked, and the button goes away when P13 turns the tab row into the toggle arc. |
| 85 | Selected Learn tab rendered off-screen; BringIntoViewRequester fix added | RESOLVED | 14c7316 - ui/src/commonMain/kotlin/com/asoc/typewright/ui/learn/LearnScreen.kt:205-207 BringIntoViewRequester + LaunchedEffect(selected) | The standard fix is committed, but it is not screenshot-verified because ScreenshotHarness renders one frame with no coroutine dispatcher; that harness limitation is still open (see notes). |
| 86 | No whole-font SfntFont to UfoProject bridge in core-font | V1-BLOCKING | P12 | Still open: core-font has only sfnt table readers and UFO I/O, with no conversion anywhere (grep finds none). P12 step 1 ('TTF/OTF → UFO through core-font') is exactly this bridge, and golden-path step 1 ('open an existing TTF/OTF') needs it. Lead: P10 adds `SfntFont.toUfoProject()` for the golden-path seeds, with no unicodes (item 124), no licence audit and no project directory; P12 finishes the import. |
| 87 | wasmJs Node resource readers fail when called from another module's test bundle | DEFERRED |  | This limitation only affects Node test bundles, and the JVM tests already cover it. What ships to users is the browser loader gap, and that is triaged under item 92. A browser-capable loader built for 92 would probably close this too. |
| 88 | Brief §4.3 margin/proof pull-down per room: design still open | DEFERRED |  | SCREENS_V1 §7 lists 'the vertical margin and proof axis' as out of V1, and D8 recommends leaving margin/proof for v1.1. It is still formally part of the owner's D8 answer. |
| 89 | Corner Workbook taskcard stands in for the missing specimen/router | V1-BLOCKING | P13 | WorkbookEntryButton and LearnEntryButton are still stacked in a corner (TypewrightApp.kt:116-117). P13 step 2 removes the two corner buttons and names 'Close OQ 56/82/89', and step 4 adds Home's taskcard. |
| 90 | Workbook demonstration row starved stats column on long labels | RESOLVED | e40d3ae: WorkbookGateDisplay.kt:72 isHeroStatLabel and WorkbookScreen.kt:341 Column(Modifier.weight(1f)); covered by WorkbookScreenScreenshotTest.task6RealGateRowsRenderAMixOfPassAndWarn (line 108) | The item records a layout bug that was found and fixed in the commit that built the screen. Nothing is left to do. |
| 91 | Judgment-call workbook tasks can never be done; task 1 always current | V1-BLOCKING | P11 | Still true: WorkbookProgress.kt campaignProgress has no persisted learner-confirmed flag, so tasks 1/2/7/8/10 never read DONE. That breaks J3 (the taskcard should say 'Task 4') and the dogfood Workbook playthrough. No prompt names this fix; P11 is nearest because it moves Workbook state into lessons/. The hard-coded sans-geometric task-4 key belongs to P12 and the task-11 compiled-font gate to P14. |
| 92 | Campaign/corpus loading fails in the browser; Workbook unavailable on web | V1-BLOCKING | P14 | The qa:corpus, campaign and learn:scenes wasmJs loaders still call process.getBuiltinModule('fs'), which a browser does not have. The web preview therefore can't run S14's layer-two checks ('14 layer-two checks on Android and web'), S15 Economy or S19. No prompt names a browser resource loader; P14 is the nearest because it builds Check/Economy and owns the web (D) section. The duplicate snapshot load is polish that P11's ProjectSession removes. |
| 93 | Workbook reflections in-memory; Trace-again unwired; designer 'Madhav' stand-in | V1-BLOCKING | P11 | Reflections still go into a remember'd list (WorkbookScreen.kt:585). P11 moves them into lessons/, and S19 is 'done when reflections persist in lessons/'. The other two parts close elsewhere: 'Trace again with Fit' in P16 step 4, and the hard-coded designer when P11 Font info and P12 replace the Hyle Deco reference project. |
| 94 | Learn screenshot tests at 210 dp, narrower than explorer phone frame | V1-BLOCKING | P13 | LearnScreenScreenshotTest and LineagesTabScreenshotTest still use width=420 at density 2. P13's acceptance ('screenshot tests for every screen above at phone and desktop width, compared against the mockups', Learn included) can't pass with them. This is a test convention, not a user-facing defect. |
| 95 | Workbook commonTest fixture used Node-only loader; ui browser tests not in CI | RESOLVED | e40d3ae: WorkbookScreenStateTest.kt:30 syntheticTask / :44 fixtureSnapshot read no resource; 3673df1 added :ui:wasmJsBrowserTest to CI (.github/workflows/ci.yml:85) | The fixture bug was fixed in the P7 commit. The CI gap the item exposed (':ui:wasmJsBrowserTest' never ran) is now closed too. |
| 96 | Devanagari template pack has two duplicate SVGs (011, 056) | MADHAV | D13 | The code already handles the duplicates (66 distinct glyphs across 68 sheet entries, generated manifest in 6eed2e9). What's left is a content decision about the owner's own template asset. |
| 97 | Kana files broke :scripts compile and ktlint during parallel P8 work | RESOLVED | bcd5264: HiraganaGlyphs.kt:25 and KatakanaGlyphs.kt:26 are 'internal val ..._TEMPLATES'; the whole-project spotlessCheck in CI (3673df1) guards ktlint | Item 98 records the fix, and the committed code shows it. |
| 98 | Kana visibility and ktlint fixes for item 97 applied | RESOLVED | bcd5264: HiraganaGlyphs.kt:25 / KatakanaGlyphs.kt:26 internal templates; scripts tests run in CI | This item only records a fix that is in the code. Nothing is left to do. |
| 99 | Kana template counts 55/57 confirmed; per-script kana-manifest.json | RESOLVED | bcd5264: scripts/templates/kana-manifest.json template_count 55 (Hiragana) / 57 (Katakana); generate_kana_manifest.py:138-168 raises on count mismatch or duplicates | This records a decision that has been taken and implemented, with nothing open. |
| 100 | Kana virtual-body metrics; only baseline marked fixed | RESOLVED | bcd5264: KanaMetrics.kt:53 baseline fixed=true, others fixed=false; KanaMetricsTest.kt:68-70 pins it | The modelling decision is implemented and tested. The item asks no question. |
| 101 | No sourced kana starting control-character set; left empty | DEFERRED |  | The V1 Workbook is the 12 Latin tasks only. The item itself says it becomes an owner decision only 'if a kana workbook path is scheduled', which is post-V1. The empty set is pinned by KanaControlCharactersTest:18. |
| 102 | Kana feature plan thin: no dakuten templates, no vert, no kern | DEFERRED |  | Kana ships in V1 only as a labelled Preview. Vertical writing and dakuten composition go beyond that, and the missing dakuten templates are a gap in the template asset to fix when kana leaves Preview. |
| 103 | Kana manifest generator's failure paths untested against broken input | DEFERRED |  | This is a test-coverage gap in a one-time build script, not in a core-* module or a V1 path. V1 can ship without it. |
| 104 | Arabic joining types hand-entered, not derived from ArabicShaping.txt | RESOLVED | 6561c85: generate_naskh_manifest.py:73-113 table (28 D / 10 R / 1 U, matched by ArabicLetters.kt); diffed now against ArabicShaping-18.0.0: 39 entries, 0 mismatches | The classification is implemented and I re-verified it against Unicode's own file. The only thing left is an optional check that runs automatically on regeneration. |
| 105 | Naskh eye/loop heights are [CONFIRM] placeholders; sky/earth naming | DEFERRED |  | Naskh is Preview in V1, and ArabicMetrics' placeholder eyeHeight/loopHeight lines are used by no ui code. Confirming them waits until Naskh leaves Preview. The sky/earth naming and the Nastaliq exclusion are already implemented (6561c85). |
| 106 | Android shaper has no cluster data; no JNI HarfBuzz in v1 | DEFERRED |  | The desktop half is done: SkikoShaper wires clusters (48293a7, SkikoShaperTest:89), and Android returns clusters=null by design (AndroidTextRunShaper.kt:110, DECISIONS D14). The remaining Android HarfBuzz binding for 'explain this conjunct' is post-V1 by the item's own scoping. |
| 107 | Android shaper ignores script and may silently use system fallback | DEFERRED |  | These gaps only affect the Preview-labelled non-Latin shaping preview on Android and need a device to verify (law 4). The Latin golden path doesn't use shape-preview. No P11-P19 prompt covers it. |
| 108 | Devanagari and kana node-economy corpus built with Google Fonts categories | RESOLVED | 3fb7fff: data/scripts/build_script_node_economy_corpus.py:36-39 classifies by the Google Fonts category; ScriptNodeEconomyCorpusTest.kt:28-34 pins 66 glyphs and the 3 Devanagari classes | A record of a taxonomy decision that is already implemented in the generator and pinned by tests, with nothing left to do. |
| 109 | Per-class family counts; thin classes under five families skipped | RESOLVED | 3fb7fff: build_script_node_economy_corpus.py:91 MIN_FAMILIES = 5; ScriptNodeEconomyCorpusTest.kt:34,62-85,150,155 pin the class sets and n=25 (devanagari-sans) and n=18 (kana-sans) | The thin-class rule (law 5) is implemented and the committed packs match the logged numbers; the item only records them. |
| 110 | Corpus loader extended; no compact pack or UI consumer for new scripts | DEFERRED |  | The loader wiring is done in 3fb7fff (NodeEconomyCorpus.kt:71,83,93), but both threads left open (a compact Devanagari/kana pack, which depends on item 8, and any UI or campaign consumer of loadDevanagari/loadKana) are non-Latin Economy work that V1 leaves out, because non-Latin scripts are Preview and J4's Check reports layer two only. |
| 111 | P8 verification pass; KanaFeaturePlan KDoc GSUB miscount fixed | RESOLVED | bcd5264 (fix folded into the kana commit): scripts/.../kana/KanaFeaturePlan.kt:16 now states Devanagari has 15 GSUB stages; 4dfb397 logs the pass | A verification record whose one defect is already fixed in committed code. |
| 112 | Web has no glyph ids or clusters; browser shaper stays stub | RESOLVED | e384d88: shape-preview/src/wasmJsMain/.../BrowserFontFaceShaperStub.kt:60-66 returns ShapeResult.NotImplemented with the re-verified finding | The decision (keep an honest stub and fake no glyphs) is implemented; this is a record with nothing left to do, and the web is a labelled preview in V1. |
| 113 | BrowserFontFaceVisualPreview renders real FontFace text to PNG | RESOLVED | e384d88: shape-preview/src/wasmJsMain/.../BrowserFontFaceVisualPreview.kt:57 (object with renderPng); BrowserFontFaceVisualPreviewBrowserTest runs in headless Chrome | Built and tested as described. The fact that nothing in ui/ calls it is a separate gap, logged in the notes. |
| 114 | Wasm suspend-test limitation worked around; padding test bug fixed | RESOLVED | e384d88: BrowserFontFaceVisualPreviewBrowserTest.kt:97-121 uses GlobalScope.promise and subtracts renderPng's fixed padding before comparing widths | Both the workaround and the test fix are committed and passing; nothing remains. |
| 115 | Hosted build endpoint wire contract is new and unreviewed | DEFERRED |  | The hosted endpoint is out of V1: D4 makes web V1 a preview with no compile, SCREENS_V1 §7 lists hosted builds as out, and LICENSING.md puts them at v1.x. |
| 116 | Hosted backend reports Available once an endpoint is configured | DEFERRED |  | This only matters with a configured endpoint, and V1 web ships with platformCompileBackend() = HostedEndpointBackend(null), which reports Unavailable. The [CONFIRM] belongs to the post-V1 hosted product under D4's revisit. |
| 117 | Hosted endpoint zero-retention is a policy, not proven | DEFERRED |  | This is a deployment-time requirement for the hosted endpoint, and V1 excludes that endpoint under D4 and SCREENS_V1 §7. |
| 118 | compile wasmJsBrowserTest registered but disabled | DEFERRED |  | Still disabled (build-logic KmpPlatformConventionPlugin.kt:49-50), but compile's only wasm code is the hosted client, which V1 web doesn't use (D4: no compile), so browser-testing it can wait. |
| 119 | Puck grip-drag never engages; hit-test uses wrong coordinate space | V1-BLOCKING | P13 | This is a real bug on every target, not a headless artifact. At HEAD, Puck.kt:201-205 puts pointerInput after offset and size, so pointer positions are puck-local (0-68 dp), while geometryNow() (Puck.kt:125) uses the canvas-space pin.center, so isWithinGrip is never true. The radial's hubCenter has the same mismatch. That breaks S11/S12 Draw, and P17 (the Draw audit and the radial) is where a reader would expect it closed, although P13's puck repositioning also touches it. Lead: the root cause (puck-local vs canvas-space positions) also breaks grip-drag on Android and desktop and probably the hold radial's hub centre, so it lands with P13, before the deck re-pins the puck. |
| 120 | P9 verification pass; web frame-count inconsistency flagged, unfixed | DEFERRED |  | The verification itself is done (af371a8). The one open thread is that WEB_PERFORMANCE.md's original table (lines 98-104) still pairs 510-520 frames with '4 real seconds', flagged at :176 but not reconciled. That is doc polish V1 doesn't need. |
| 121 | google/fonts states no licence for tags/ | MADHAV | D14 | families.csv is no longer committed (3673df1); whether using it at build time needs permission is the owner's call. |
| 122 | Pre-split app code is Apache-2.0 for anyone who copied it | MADHAV | D1 | A consequence of the licence history, for the lawyer review LICENSING.md already asks for. |
| 123 | V1 mockup sources ui/v1-screens/ are not in the repository | MADHAV | D7 | Law 6 makes them the look source for every V1 screen; P11's S01/S02/S06 UI needs them first. |
| 124 | Glyphs carry no Unicode code points; GlifCodec drops <unicode> | V1-BLOCKING | P11 + P12 | No V1 prompt names it: without unicodes an imported font loses its cmap and a compiled font maps no characters (golden-path steps 1a and 5). |
| 125 | The UFO writer is cubic-only; a TTF-opened project can't be saved | V1-BLOCKING | P11 + P12 | Found by the golden-path harness in P10: writeGlif refuses quadratic contours (GlifCodec.kt:247), so no imported TrueType font can be written as a project. |
