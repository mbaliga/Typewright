# ui

The Compose Multiplatform UI for Android, desktop and web, reproducing `ui/typewright-explorer.html` (one sheet, rooms, puck, bloom, glass) as `UI_SPEC.md` names it. Its desktop tests hold a headless screenshot harness that renders composables to PNG under `ui/build/screenshots/` for comparison with the explorer's renders.
