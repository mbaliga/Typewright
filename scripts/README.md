# scripts

Per-script data for Latin, kana, Devanagari, Arabic and the scripts after them: metric systems, glyph inventories with correct names, control strings and feature order (handoff M8). The capture template sheets for six scripts live beside it in templates/.
