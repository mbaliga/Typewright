# qa/corpus

The node-economy reference data and the style detector: per-style, per-glyph point-count distributions measured from top Google Fonts faces, and the fence that calls a glyph outlier, above or in range (brief §8). Its data is copied from data/node-economy-*.json into generated resources at build time and is never hand-edited.
