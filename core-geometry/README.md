# core-geometry

Pure Kotlin geometry for Typewright: points, cubic and quadratic curves, contours, curve fitting, metrics and node-economy counts, all in font units with y up. It has no platform dependencies and is where correctness lives, so every public function gets a unit test.
