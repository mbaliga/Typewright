# engine-construct

Construction geometry for drawing a font from nothing: booleans, offsets, stroke to outline, spiro or Hobby splines, transformations and parametric primitives (handoff M2, brief §10). It is pure Kotlin with no platform dependencies and is exhaustively unit tested.
